[build]
buildCommand = "npm run build"

[deploy]
startCommand = "PORT=${PORT:-3000} node .next/standalone/server.js"
healthcheckPath = "/api/health"
healthcheckTimeout = 120
restartPolicyType = "on_failure"
